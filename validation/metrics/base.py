"""Metric interface over (gold dataset, solution output).

Metrics are stateless functions mapping (GoldDataset, SolutionOutput) to a
numeric or boolean value, exposed through the shared helpers in metrics.core.
"""
